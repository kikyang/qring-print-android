<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 12+ 蓝牙运行时权限；12 以下用 BLUETOOTH/BLUETOOTH_ADMIN -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <!-- OTA 检查更新/下载 APK（2026-08-13 补：v0.5.2 起缺这行，release 包无 INTERNET 权限
         导致 OTA 全部失败——AGP debug 构建自动注入该权限、release 不注入，所以只在 release 暴露） -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- OTA 安装 APK（2026-08-13 补：Android 13+ ACTION_VIEW 装 APK 必须声明此权限
         + 用户授权「允许安装未知应用」，否则最后一步报无权限安装） -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:label="错题小印打印"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.Material.Light">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|uiMode|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- #14 系统分享入口（2026-08-27）：相册/文件管理器「分享图片」直接进入图片页 -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DebugActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <!-- OTA 更新安装（2026-08-12）：暴露 cacheDir/updates/ 下的 APK -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
