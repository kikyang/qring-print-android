# 错题小印打印机逆向 待办事项清单

> 建立: 2026-08-13 | 四象限管理 (重要×紧急) | 模板: docs/待办清单模板.md
> 2026-08-18 会话更新(二十八): **v0.7.3 已发布 + README 截图更新**——三种界面主题明显差异化（按钮/圆角/选中态随主题变化）；README 界面预览更新为 v0.7.3 实机截图，并新增三套主题对比图；隐私处理：不截「我的」页（含蓝牙配对列表/MAC）。
> 2026-08-18 会话更新(二十七): **v0.7.2 已发布**——图片工作台重构、手动裁剪、PPT 导入、公式排版/自研 Word 排版引擎、多份打印、多界面主题全部完成；142 测试全过，R8 APK 0.95MB；已推送 main + tag v0.7.2 + GitHub Release。
> 2026-08-18 会话更新(二十六): **v0.7.1 已发布**——新增 OTA 升级后更新说明弹窗（跨版本收集更新日志）；137 测试全过，R8 APK 0.90MB；已推送 main + tag v0.7.1 + GitHub Release。
> 2026-08-18 会话更新(二十五): **用户确认 UI/交互重构路径**——首页 → 开始打印 → 快速通道（文字/条码/文档）+ 图片工作台（图片/错题卡/拍照/分享）+ 模板生成器 + 自由设计（画布）；所有路径最终汇聚到「统一准备打印页 + 统一打印确认条」。图片工作台为本轮重构重点，吸收错题卡、模板、画布入口，并新增手动裁剪。
> 2026-08-17 会话更新(二十三): **记录下一版规划（暂不实施）**。用户确认下版加入：① PPT 导入；② 公式排版 + 自研 Word/PPT/Excel 解析排版引擎（参考 yiran168/suda-win-web）；③ 多份打印。另记录 UI 偏好：用户喜欢 xyprt_android 的简洁美观界面（2026-08-11 原话），随后选择”微信风”落地为当前 UI；未来可考虑多界面/主题可选（微信风 / xyprt 简洁风 / 仿喵喵机蓝白风等）。
> 2026-08-17 会话更新(二十四): **OTA 更新说明弹窗已实现，随累计更新一起发（不发独立 0.7.1）**。装好新包后下次启动弹出「更新说明」：内置更新日志 ReleaseNotes.kt（纯 Kotlin 可单测）+ Settings.lastSeenVersion 记录上次运行版本，版本区间收集（跳版本也列出）；新增 ReleaseNotesTest 5 例，全量 **137 测试全过**。代码已就绪待下版，⚠️ 发版时需在 ReleaseNotes.LOG **顶部新增当前版本说明**（与 version.json notes 同步）
> 2026-08-16 会话更新(二十二): **图片增强升级已实现并发布 v0.7.0**。图片页高级设置新增增强算法（Sauvola/Wolf/Bradley）与增强强度（弱/标准/强），一键增强改为高分辨率光照补偿 → 384 缩放 → 三算法二值化；新增 ImageEnhancerTest 5 例，全量 **132 测试全过**，R8 release APK 915K。同日上游巡检 8-16：lztttt v1.5.0 为本次功能来源；BA4RFY 源码确认（CSV/标签观察）；Thisko Web 移除观察。报告 docs/upstream-audit-2026-08-16.md
> 2026-08-14 会话更新(二十): **照片旋转+缩放 + Markdown 打印已实现**（用户确认吸收，计划批准后落地）。图片页新增旋转 0/90/180/270（仅旋转，保持自动适应 384 宽）+ 缩放滑杆 50%~200%（>100% 中心裁边放大细节、<100% 白底居中缩小留白），rotate/scale 入参数记忆+历史再编辑可恢复；文档 Tab 内新增 Markdown 卡片（📋粘贴/📄选 .md/🖨打印），自写 MarkdownParser（纯 Kotlin 零依赖，JVM 全测）+ MarkdownRenderer（384 宽白底位图走图片通道 mode=2），支持标题/列表/加粗/斜体/行内码/链接/代码块/引用/分割线，未引第三方依赖。新增 28 测试（ImageTransform 8 + MarkdownParser 13 + MarkdownRenderer 5 + UiTest 2），runUnitTests 全量 **127 全过**。runPreviewCheck 真机自检（旋转/缩放五张 + markdown_all 混排）待设备连接
> 2026-08-14 会话更新(十九): **上游巡检 8-14**：lztttt 8-13/8-14 连发 v1.3.0/v1.4.0（表格/Markdown/错题本/PDF/批量/课程表横版，**代码级核查全部真实实现**，修正 8-13"亮度/锐度未实现"勘误——现 ImageTransform 已实现但价值中低不并入）；新发现 ZhaYi-Miao 桌面端+Web 控制台（HTTP 远程打印/LaTeX，观察）、tanadiejiang/pocket_print（Flutter，分享图片入口值得吸收）。报告 docs/upstream-audit-2026-08-14.md。**本轮新增规划**：Q2 加 Markdown 打印+批量打印，Q3 加横版照片旋转推荐+系统分享图片入口
> 2026-08-13 会话更新(十八): **v0.6.3 发布：修复 OTA 最后一步无权限安装**。用户实测 OTA
> 下载/更新通路已通，但系统弹「没有权限安装」→ 根因 = **Android 13+ ACTION_VIEW 装 APK
> 需 REQUEST_INSTALL_PACKAGES 权限 + 用户授权「允许安装未知应用」**（manifest 一直缺）。
> 修复：manifest 补权限；UpdateManager 安装前 canRequestPackageInstalls() 检查，未授权自动跳
> ACTION_MANAGE_UNKNOWN_APP_SOURCES 设置页，首次授权后免重复。99 测试全过，release 权限
> 双验（REQUEST_INSTALL_PACKAGES+INTERNET）。tag v0.6.3 + gh release + @v0.6.3 APK 探针 200；
> data API 收录延迟待复探
> 2026-08-13 会话更新(十七): **v0.6.2 发布：连接固定 SPP 直连（修条码发淡根因）**。用户实测
> 条码明显比黑白格淡 → 根因 = AUTO 在 SPP 失败时**静默回退 BLE**（无感知但打印慢、墨色淡）。
> 定案：Settings.connectionMode 默认/兜底 SPP，主界面强制 SPP；已连接状态显示**真实通道**
> （SPP/BLE，替换误导的「蓝牙版本 BLE」——X1 对 10 FF 30 10 返回固定 "BLE"）；扫描列表去
> 「（BLE 直接连接）」硬编码。**设备名可自带 BLE 后缀（Qring_X1_BLE），是真实名不清洗**。
> 手机实测 SPP 生效：测试页比早上黑不少；比大字/黑白格略淡=全黑块残热积累差异，正常，
> 要更黑靠浓度 2。commit 18d62cf（代码）+ b288964（v0.6.2 发版）+ tag v0.6.2 + gh release
> 页面建好 + jsDelivr data API 已收录 0.6.2，手机 OTA 可直接升级。99 测试全过
> 2026-08-13 会话更新(四): #5 a/b/c/d 完成（94 例测试全过）：历史再编辑、参数记忆、
> 图片页参数分层+设置收敛+去专业说法、统一画布（涂鸦并入元素画布 KIND_DRAW）；下一步 #5e 模板系统打通
> 2026-08-13 会话更新(五): #5e 模板系统打通完成（97 例测试全过）：系统模板改为内置 JSON 注册表
> （SystemTemplates.kt 数据驱动，不再硬编码宫格）；用户画布模板显示在「我的模板」宫格
> （缩略图 + 点击进画布编辑 + 长按删除），存/删模板即时刷新；下一步 #5f 首页与打印页分工
> 2026-08-13 会话更新(六): #5f 首页与打印页分工完成，**#5 功能减法整合 6 条全部落地**（98 例测试全过）：
> 首页=启动台（设备状态 + 开始打印/打印历史两个入口 + 使用提示），打印页=工作区
> （5 个二级 Tab 收齐全部打印功能），删除首页重复的 5 功能宫格，使用提示文案同步更新。
> #5 行 a-f 全部 ✅；后续按用户约定进入硬件联调（只剩 GATT 写特征 + 热敏头物理行为两个未知量）
> 2026-08-13 会话更新(七): **#11 功能实测 + 联调记录收尾全部完成**。实物联调全链路通过：
> 两个物理未知量（GATT 写特征 FF02/FF01 + 热敏头行为）已实测验证；功能实测收尾补测
> 错题卡复习友好版（无图版式骨架，用户确认正常）、条码 QR、文字大号+加粗+居中、文档 PDF
> （PdfRenderer 链路 252 行）；联调记录表已填 docs/实物联调手册.md（含协议核对字节/浓度定稿 2/
> 功能汇总）；#3/#4 标 ✅；SPP 通道实物测试挂 Q2 #9（需系统配对）；#8 错题卡融入模板待续
> 2026-08-13 会话更新(八): **SPP 颜色偏深根因确认 + 通道策略反转**。SPP 比 BLE 黑=传输快
> （1024B/1ms）→行间隔短→打印头残热积累（非纸的问题），命令序列/浓度 10 FF 10 00 02 两通道
> 一致；SPP 查询实测也可响应（早期"单向"系 BLE 占用时连接失败的假象）；**定案：AUTO 默认
> SPP 优先、BLE 兜底**，PrinterHolder.connect 已实现切通道先断开（99 例测试全过）
> 2026-08-13 会话更新(十): **SPP 通道 ENABLE2 + 光栅整发 PC 实测**（用户质疑旧结论全是 BLE
> 通道实测的，要求先 PC 上试；Windows 配对 Qring_B8EA 得传出 COM4，pyserial 操作）：
> ① ENABLE2(1F B2 10) A/B 对照坐实 **SPP 同样渲染「固」字**，任何通道禁发，QringProtocol
> 注释保持正确；② 光栅整发 **200 行干净、400 行满纸「固」** → 固件光栅单块行数上限约 255
> 行（BLE 256 行「固字瀑布」同因）→ PrintJobRunner **64 行分块两通道安全，无需整发/通道
> 差异化，不改代码**。教训：最小步进被违反（200→400 跳档，满纸固字费纸，用户喊停关机），
> 已强化 feedback-print-debug-minstep 记忆（每次扩大量明确告知纸量）
> 2026-08-13 会话更新(十一): **「固」字机制定论（回答"为啥别人没遇到"）+ 三上游源码核对**。
> qrintprint 鸿蒙 / lztttt Android / snowboys Windows 三个上游**全发 ENABLE2、光栅全整发
> 不分块**（时序逐行核实：qrintprint sendAll([ENABLE,ENABLE2])；lztttt PrinterConnection.kt:485；
> snowboys device.py:322 enable()=ENABLE+ENABLE2）。→ 固字两触发源它们全踩中：打印开头每张
> 一个「固」落留白区没人看；打 >255 行长图瀑布（顶部正常底部固字 → 被误判纸/机器问题，零 issue）。
> 铁证：lztttt CanvasDocument.kt 作者记过"打出来一堆乱码"但归因成 16 位高度回绕、设 4000 上限，
> 拦不住 ~255 行真因。**分块不是固字成因而是解法**（64 行分块避超限 + 禁 ENABLE2 避文本引擎）。
> 工程结论不变：PrintJobRunner 不改，比上游更稳。已沉淀进 README
> 2026-08-13 会话更新(十二): **SPP 提速到块间 0ms（三档阶梯实测落地）**。用户质疑
> APP 清晰而 PC 测试稍差（纸还是速度？）→ 同纸同图案只变块间 delay 对照：150ms 基线 /
> 30ms 完整且更清晰 / 0ms 完整（仅开头几行缺色=打印头冷启动物理现象，与速度无关）。
> 结论：传输快→行间隔短→残热积累墨色更实，SPP 块间延迟可归零。代码落地：PrinterIo 加
> rasterChunkDelayMs 通道属性，SPP=0ms / BLE=150ms（传输慢保留）/ Fake=0ms，
> PrintJobRunner delay(150)→delay(io.rasterChunkDelayMs)，99 测试全过。SPP 一页省 ~750ms
> 2026-08-13 会话更新(十三): **SPP 0ms 浓度有效性实测 + 全参数体系核验**。① 用户问
> 「现在打印很黑，浓度还有影响吗」→ 50% 密度棋盘 0/1/2 三档对照：**浓度仍有效**
> （0 淡像缺色不建议、1 适中、2 浓），SPP 打太黑调 1 即可；② 完整图像参数体系
> （增强/消除笔/对比度/黑白深浅/线稿 + 抖动三模式 + 布局 + 浓度）在很黑下核验：
> 除浓度（墨点深度）外全部操作光栅黑白分布/排列/布局，SPP 残热只加深黑点不抹平
> 分布 → 全部照常体现；两个正交旋钮：浓度 1~2（深浅）+ 阈值/对比度（黑白分布）。
> 顺带澄清：开头缺色=全黑块固件电流限制假象（非冷启动），预热条方案 protocol.md
> 早已实测排除（文字本就黑 + 顶部白黑条怪象），正常内容无需预热
> 2026-08-13 会话更新(十四): **v0.6.0 生成上传 + 手机 OTA 可直接升级**。SPP 块间 0ms +
> SPP 优先 + 功能减法整合 6 条 + 虚拟打印机端到端全在版内；99 测试全过、R8 0.88MB；
> version.json 0.6.0 + releases APK 替换 + tag v0.6.0 推送（commit 88ed7fd）。jsDelivr
> 三探针秒级同步（@main version.json / data API 已收录 0.6.0 排最前 / @v0.6.0 APK 200 且
> 字节与本地一致，本次无 2-3h 滞后）。README 整体更新（新增更新日志段 + 下午踩坑记录：
> 开头缺色=全黑块电流限制假象非冷启动、浓度命令无响应属正常、**用户确认不做预热条**
> 实际内容黑靠浓度加深、150ms 是打印时间瓶颈）
> 2026-08-13 会话更新(十五): **v0.6.1 修复 release OTA 失败 + GitHub Release 页面补齐**。
> 手机 v0.6.0 检查更新弹「网络异常」根因 = **AndroidManifest 缺 INTERNET 权限**（AGP debug
> 自动注入、release 不注入，v0.5.2 起 release 包 OTA 全失败；蓝牙不需要 INTERNET 故未暴露）。
> 修复+手机实测「已是最新」验证通过。**补 gh release create v0.6.1**（v0.6.0 只 push 了 tag
> 没建 Releases 页面条目，用户逛 GitHub 页面 Latest 还是 v0.5.5）。版本号从 0.6.0 升 0.6.1
> 原因：0.6.0 tag 已固化坏 APK，@v0.6.0 下载会给坏包，必须新 tag。commit 3c3d436 + tag v0.6.1。
> 记忆已更新（INTERNET 坑 + gh release 页面坑入 pre-release 清单）
> 会话更新(十六)：**README 界面预览更新到 v0.6.1**（commit 3700ff2）。6 张真机截图
> 对应当前 5 个二级 Tab（首页/文字/图片/条码/文档/其它），旧 v040 图删除。**隐私规避**：
> 「我的」页显示系统已配对蓝牙设备真实 MAC+名称（pm clear 清不掉），已弃用该页截图，
> README 加说明。手机 App 数据已被 pm clear（历史/设置需重配，蓝牙配对是系统级未动）
> 2026-08-13 会话更新(九): **BLE 分包手机扫档定稿 + BLE 藏进调试（主力 SPP）**。手机实测：
> 96B/40ms 稳定、128B 卡死（Android 蓝牙栈对大包行为差异，电脑端 128B 打 407 行都稳），
> 固化为默认（BLE_CHUNK_DEFAULT=96 / DELAY=40，比旧 32B/80ms 快 6 倍）；主界面移除连接方式
> 选择（固定经典蓝牙 AUTO=SPP优先BLE兜底），BLE 连接入口藏进调试台（DebugActivity BLE 连接区块，
> PrinterHolder 加 connectBle，扫档前先连 BLE）；SPP 与上游 HarmonyOS 参考（reference/qrintprint）
> 核对：UUID/1024B+1ms 分包一致，避开 ENABLE2（X1 渲染"固"字）与光栅整发（256 行瀑布）两个坑
> （99 例测试全过）

## 下一版统一交互路径（2026-08-18 已确认）

```
首页
 └─ 开始打印
     ├─ 快速通道：文字 / 条码 / 文档
     ├─ 图片工作台：图片 / 错题卡 / 拍照 / 分享 / 手动裁剪
     ├─ 模板：一键生成后回到上述流程
     └─ 自由设计：画布，完成后回到统一准备打印页
 └─ 打印历史
 └─ 我的
```

所有打印路径最后汇聚到：

- 统一准备打印页：实时所见即所得预览 + 当前内容相关操作
- 统一打印确认条：份数 / 浓度 / 走纸 / 打印

## Q1 重要且紧急(当前主线)

| # | 事项 | 状态 | 阻塞/备注 |
|---|---|---|---|
| 3 | APK 实物联调 | ✅ | **2026-08-13 全部完成**：功能实测（文字 / 字号加粗对齐 / 图片一键增强 / 错题卡复习友好版 / 条码 QR / 文档 PDF / 自检页 / 浓度校准）全过，联调记录已填 docs/实物联调手册.md 末尾记录表；SPP 通道实物测试单独挂 Q2 |
| 4 | 打印协议验证 | ✅ | X1 实测与开源逆向完全一致（状态/电量/设备信息/走纸/唤醒/使能/浓度 0~2；ENABLE2 禁发），详见联调手册记录表 |

## Q2 重要不紧急(规划推进)

| # | 事项 | 状态 | 阻塞/备注 |
|---|---|---|---|
| 9 | SPP 通道实物测试 | ✅ | **2026-08-13 实测通过并定案**：SPP 可用且**查询也可响应**（`10 FF 40/50/70` 正常返回——早期"单向、查询无响应"系 BLE 占用时连接失败的假象）；**SPP 比 BLE 更快更黑**（传输快 → 行间隔短 → 打印头残热积累，非纸的问题；浓度命令 `10 FF 10 00 02` 两通道一致）；关键教训：**打印机同时只接受一个连接**（BLE 占用时 SPP 被拒，日志 `read failed: socket might closed or timeout`）；**定案：AUTO 默认 SPP 优先、BLE 兜底**，`PrinterHolder.connect` 已实现切通道先断开当前连接（99 例测试全过） |
| 5 | **功能减法整合**（概念收敛/入口串联） | ✅ | 用户已确认全部 6 条（a-f）都做；另追加"设置项收敛+文案通俗化"并入 c。a) ✅ 历史"再编辑"（参数 JSON 已存，恢复编辑页）；b) ✅ 参数按内容类型记忆（浓度全局、抖动/增强按文字/图片/条码分存）；c) ✅ 图片页参数分层（默认只露增强+抖动+浓度，排列/消除笔/裁白边/黑白深浅/线稿收"高级"折叠；去专业说法：清晰/细腻/高对比、线稿模式、黑白深浅等；我的页走纸也收折叠）；d) ✅ 统一画布（涂鸦并入元素画布 KIND_DRAW，图片页单入口「🖌 画布」，笔画随模板持久化；DrawCanvasView 已删）；e) ✅ 模板系统打通（系统模板=内置 JSON 注册表 SystemTemplates.kt，宫格数据驱动；用户模板显示在「我的模板」宫格，缩略图+点击进画布编辑+长按删除）；f) ✅ 首页与打印页分工（首页=启动台：设备状态+开始打印/打印历史+使用提示；打印页=工作区：5 个二级 Tab 收齐全部打印功能；删除首页重复的 5 功能宫格）。**错题卡不升格**：降级为普通模板，模板打磨已由 #7 完成 |
| 6 | 逆向成果文档化 | ✅ | docs/protocol.md（完整协议）、docs/architecture.md（客户端结构）、README 已整理 |
| 8 | **错题卡融入模板系统** | ⏳ | 用户 2026-08-13 联调时提出（内部功能合并方向）：错题卡改为模板形式，与常用模板/我的模板共用宫格和编辑流程，入口统一（画布↔模板进一步合一）|
| 7 | **错题卡模板「复习友好版」** | ✅ | 完成（2026-08-13）：buildReview（进度栏+题目图前+撕纸线+订正区后）与 buildReworkSheet（N 题卷子）已落地，8 个模板测试通过。版式用 PIL 真栅格化验证（Robolectric 原生图形不渲染文字），**顺带修掉 v0.5.5 标准卡潜在 bug**：举一反三标签与订正末行压线（调用处多加一个 LINE_GAP 留白）。预览 PNG 在 temp/tpl_pil_*.png。视觉终验留到联调 |
| 11 | **Markdown 打印**（上游 lztttt v1.4.0） | ✅ | **2026-08-14 已实现**（会话更新二十）：文档 Tab 内 Markdown 卡片（📋粘贴/📄选 .md/🖨打印），自写 MarkdownParser（纯 Kotlin 零依赖）+ MarkdownRenderer（384 宽白底走图片通道），支持标题/列表/加粗/斜体/行内码/链接/代码块/引用/分割线；历史类型 Markdown 可再编辑。走自研方案未引第三方依赖，解析测试 13 例 JVM 全过 |
| 12 | **批量打印**（上游 lztttt v1.4.0） | ⏳ | 2026-08-14 巡检新增：txt/MD/图片多选，统一预设一次应用，逐项进度+失败跳过+清单存历史可重打。我方已有 PrintJobRunner/History 基础设施。中工作量 |
| 15 | **照片旋转+缩放**（用户提出） | ✅ | **2026-08-14 已实现**（会话更新二十）：图片页旋转 0/90/180/270（仅旋转，保持自动适应 384 宽）+ 缩放 50%~200%（>100% 中心裁边放大细节、<100% 白底居中缩小留白），rotate/scale 入参数记忆+历史再编辑可恢复；默认 0°/100% 返回原实例零回归。runPreviewCheck 真机目检（5 张变换图）待设备 |
| 16 | **图片增强升级：高分辨率灰度归一化 + 三算法可选**（上游 lztttt v1.5.0） | ✅ | **2026-08-16 已实现并发布 v0.7.0**：图片页高级设置新增增强算法（Sauvola/Wolf/Bradley）+ 强度（弱/标准/强），一键增强改为高分辨率光照补偿→384 缩放→三算法二值化；新增 ImageEnhancerTest 5 例，全量 132 测试全过，R8 APK 915K |
| 17 | **PPT 导入** | ✅ | **v0.7.2 已实现**：文档 Tab 支持 .pptx，PptxTextExtractor 提取每页文本打印；后续可补图片/版式还原 |
| 18 | **公式排版 + 自研 Word/PPT/Excel 解析排版引擎** | ✅ | **v0.7.2 已实现简化版**：MathLayout（OMML 分数/上下标/根式/括号/大型运算符/矩阵）+ DocxLayoutExtractor + DocLayoutRenderer；后续完善分栏/图片/复杂样式/真实表格线 |
| 19 | **多份打印** | ✅ | **v0.7.2 已实现**：打印确认弹窗支持 1~10 份，PrinterConnection.printRasterCopies 逐份打印 |
| 20 | **多界面/UI 主题可选** | ✅ | **v0.7.2 实现 + v0.7.3 差异化**：我的页可切换微信风 / xyprt 简洁风 / 仿喵喵机蓝白风；按钮/圆角/选中态随主题变化 |
| 21 | **图片工作台重构** | ✅ | **v0.7.2 已实现**：全屏实时预览 + 多图缩略图条 + 单图编辑 + 底部 Tab（常用/效果/排版/更多） |
| 22 | **手动裁剪** | ✅ | **v0.7.2 已实现**：ImageCropDialog 支持自由/1:1/3:4/4:3；当前作用于选中图片；相对坐标持久化待后续完善 |
| 23 | **统一准备打印页 + 统一打印确认条**（已确认主线） | ⏳ | 部分完成：v0.7.2 打印确认弹窗已统一份数；但文字/条码/文档/其它尚未完全合并到同一准备页，后续继续 |
| 24 | **模板系统改为生成器**（已确认主线） | ⏳ | 课程表/单词表/每日计划/口算题/错题卡/我的模板均作为快捷生成入口，生成结果回统一打印流程，不再维护独立复杂页面 |
| 25 | **自由设计（画布）作为可选高级模式** | ✅ | 图片工作台已保留「🖌 画布」入口，可作为可选高级模式；输出回图片通道打印 |

## Q3 紧急不重要(快速处理)

| # | 事项 | 状态 | 阻塞/备注 |
|---|---|---|---|
| 13 | **横版照片旋转推荐**（上游 ltttt v1.4.0） | ⏳ | 2026-08-14 巡检新增：图片打印检测宽>高时弹推荐一键旋转 90°（长边沿出纸方向），避免矮横条丢细节。**手动旋转已由 #15 落地**（0/90/180/270 选择器），仅剩自动检测宽>高 + 一键推荐弹窗。小工作量 |
| 14 | **系统分享图片入口**（tanadiejiang/pocket_print） | ⏳ | 2026-08-14 巡检新增：manifest 加 ACTION_SEND 接收，其他应用分享图片直接进打印页（单图进打印页、多图进列表页）。我方目前无分享接收。小工作量 |

## Q4 不重要不紧急(有空再做)

| # | 事项 | 状态 | 阻塞/备注 |
|---|---|---|---|
| 10 | **存量 0.6.1/0.6.2 升级死结（已记录）** | 🔒 关闭 | 当前版本已到 0.7.x，0.6.3 起权限已入 manifest；该问题仅影响 0.6.1/0.6.2 存量用户，不再排期，保留记录备查 |

## 已完成(近期, 供追溯)

- ✅ **照片旋转+缩放 + Markdown 打印**（2026-08-14）：图片页旋转 0/90/180/270 + 缩放 50%~200%（>100% 中心裁边、<100% 白底居中留白，默认 0°/100% 返回原实例零回归）；文档 Tab Markdown 打印（自写 parser+renderer 走图片通道，未引依赖）。新增 ImageTransformTest 8 + MarkdownParserTest 13 + MarkdownRendererTest 5 + UiTest 2，runUnitTests 全量 127 全过
- ✅ **虚拟打印机仿真**（2026-08-13）：FakePrinter 协议引擎 + FakePrinterConnection + 端到端
  测试 35 例（71→74 全过）。打印时序抽取为 PrintJobRunner（BLE/SPP/Fake 三通道共享同一份代码），
  PrinterHolder 加 injectForTest；AUTO 探测回退、缺纸/开盖/打印中故障帧、SPP 单向、
  ACK 超时链路全在 JVM 覆盖。实物联调只剩 2 个物理未知量（GATT 写特征 + 热敏头行为）
- ✅ **图像管线性能基准**（2026-08-13）：1M 像素 FS 抖动 14.3ms / 384×10000 长文档 28.5ms，
  结论无需优化（瓶颈在 BLE 传输不在像素循环），结果已入 README
- ✅ 协议逆向：QrintPrint 开源逆向确认
- ✅ 安卓客户端（Kotlin）构建出 APK（v0.5.5，0.87MB，36 测试全过）
- ✅ 构建环境搭建（JDK17+SDK34+Gradle8.7 装于 tools\）
- ✅ 上游巡检：lztttt v1.2.0 三滑块经代码级核查为宣传未实现（勘误已入报告）；全部上游 8-12 后静止
